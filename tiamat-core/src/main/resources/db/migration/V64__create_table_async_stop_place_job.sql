CREATE TABLE async_stop_place_job
(
    id               BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    status           INTEGER,
    reason           TEXT,
    created_ids      JSONB,
    created_by       TEXT,
    created_at       TIMESTAMP WITHOUT TIME ZONE,
    claimed_at       TIMESTAMP WITHOUT TIME ZONE,
    principal_claims JSONB,
    PRIMARY KEY (id)
);

ALTER TABLE async_stop_place_job OWNER TO tiamat;
